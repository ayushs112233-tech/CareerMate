# CareerMate AI — Core Dependencies
# Python 3.13 + Django + PostgreSQL Stack
# Supports SQLite for local development (set DB_ENGINE=sqlite3 or leave default)

Django>=5.1,<6.0
psycopg[binary]>=3.2.0
python-dotenv>=1.0.1

# AI & NLP Dependencies
PyPDF2>=3.0.0
python-docx>=1.1.0
scikit-learn>=1.3.0
